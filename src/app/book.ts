export class Book {
  
    title:string
    bookNumber:number
    pages:number

    constructor(title:string, bookNumber:number, pages:number) {

      this.title = title
      this.bookNumber = bookNumber
      this.pages = pages
    }
    
  }